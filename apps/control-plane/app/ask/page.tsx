import { Placeholder } from '@/components/placeholder';

export default function AskPage() {
  return (
    <Placeholder
      title="Ask"
      description="Plain-English questions over your posture graph, e.g. “show everything internet-exposed running as root”."
    />
  );
}
