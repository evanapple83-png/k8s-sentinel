import { Placeholder } from '@/components/placeholder';

export default function ReportPage() {
  return (
    <Placeholder
      title="Report"
      description="Audit-ready report with one-click PDF / Markdown / JSON export."
    />
  );
}
