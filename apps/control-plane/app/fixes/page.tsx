import { Placeholder } from '@/components/placeholder';

export default function FixesPage() {
  return (
    <Placeholder
      title="Fixes"
      description="Reviewable remediation proposals. Approvers open them as PRs — the agent never applies changes in-cluster."
    />
  );
}
