import { Placeholder } from '@/components/placeholder';

export default function PermissionsPage() {
  return (
    <Placeholder
      title="Permissions"
      description="What this agent can see, in plain English — rendered live from the actual read-only ClusterRole. Every change is audited."
      note="Plain-English capability view and opt-in elevation toggles are wired in step 1F."
    />
  );
}
