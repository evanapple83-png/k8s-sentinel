import { Placeholder } from '@/components/placeholder';

export default function PathsPage() {
  return (
    <Placeholder
      title="Attack Paths"
      description="Correlated, reachability-ranked attack chains — exposed → running → vulnerable → over-privileged → secret-access."
    />
  );
}
